def arithmetic_arranger(problems):
    length = len(problems)
    for item in problems:
        sub_items = item.split()
        first_op = first_op.append(subitems[0])
        operand = operand.append(subitems[1])
        second_op = second_op.append(subitems[2])
        
    

    return arranged_problems
